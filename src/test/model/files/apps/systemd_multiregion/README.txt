Three systemd units, varying regions:

- `every-region`: defined globally, active in all regions
- `orbit-region`: defined globally, deactivated in `us-east-1`.
- `other-region`: defined only in `us-east-1`

